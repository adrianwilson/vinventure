# VinVenture Lambda - Default Environment Variables
# Non-secret config that is safe to commit.
# Secrets go in .env.local (gitignored).

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vinventure

# Environment
NODE_ENV=development

# AWS Configuration
AWS_REGION=us-east-1
